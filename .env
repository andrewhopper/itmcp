# Pandora's Shell Configuration

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=192.168.0.1,localhost,127.0.0.1,example.com,test-server.local

# Allowed directories (comma-separated)
ALLOWED_DIRECTORIES=/tmp,/var/log,/home/admin/logs,/etc/config,/opt/data

# Allowed remote commands (comma-separated)
ALLOWED_REMOTE_COMMANDS=ls,cat,grep,head,tail,df,du,uname,uptime,whoami,id,date,hostname,ifconfig,ip,netstat,ps 